# Notebook eval directory

The notebook registers `week2_repaired_week_lookup` dynamically with its local ADK runtime. Its eval set is stored beside this file so learners can inspect and run it from ADK Web.
