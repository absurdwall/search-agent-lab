# Notebook eval directory

The notebook registers `week2_vague_week_lookup` dynamically with its local ADK runtime.

This directory exists so ADK Web can open the app's **Evals** tab without reporting that its eval directory is missing. The vague-tool experiment intentionally has no saved eval case.
